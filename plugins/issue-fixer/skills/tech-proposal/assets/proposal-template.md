<!--
技术方案报告 · 空骨架（标准档）
- 拷贝后按 references/section-model.md 逐节填；轻量档删掉不涉及的节，重型档插入加分项。
- 分层裁剪：只留改动触及层的章节（见 section-model.md §二）。
- 每节守高密度铁律：结论先行 / 量化 / 每决策配「为什么」/ 非目标必写 / 变更点显眼标注。
- 金字塔（references/pyramid.md）：写执行摘要 / 每节标题下给 `> 一句话` / 标题携带结论 / 背景走 SCQA。
- 图表（references/diagram-cookbook.md）：message-first 选图、一图一结论、关键图有条件升级白板、小图用 ASCII。
- <占位符> 全部替换；不适用的整节删除，不要留空章节。
-->

# <方案标题（体现目标，不体现方案）>

| | |
|---|---|
| 状态 | Draft / 待团队评审 / 已评审 |
| 作者 · 日期 | <作者> · <YYYY-MM-DD> |
| 范围 | <触及的包/服务/层> |
| 关联 PRD / issue | <链接> |
| 关联分支 · 协作方 | <feat/xxx> · <前后端/QA/PM @人> |

> **执行摘要**
> - **做什么**：<一句话方案（= §1 目标里的 A）>
> - **为什么**：<一句话动机（= §1 背景的冲突→疑问）>
> - **影响面**：<触及包 / 是否动契约·DB·钱 / 灰度与回滚一句话>
> - **要你拍**：<本次评审要决策的 1–3 件事，见 §11>

---

## 1. 背景与目标

> 一句话：<方案一句话 + 预期收益数字>

<!-- 走 SCQA：背景=情景S+冲突C，目标=疑问Q+回答A（见 references/pyramid.md §二） -->

**背景（现状 S + 冲突 C）**：<现状事实带数据（QPS/延迟/错误率/用例数）+ 打破现状的问题；代入评委视角补上下文>

**目标（疑问 Q + 回答 A）**：<由冲突引出的待解问题 + 方案一句话；与问题一一对应，可量化、有预期收益数字>

**非目标 / 范围**（MECE 分档）：
- ✅ 本期做：<...>
- ⏭️ 延后（可独立开发、不阻塞）：<...>
- ❌ 不做：<... + 一句话为什么>

## 2. 关联文档
- PRD：<链接> · 设计稿：<链接> · 协作方方案：<链接> · 时间节点：<...>
<!-- 每条配一句话说明它跟本方案的相关点 -->

## 3. 方案：<一句话方案，如"用 provider 抽象替换 3 处硬编码，改动收敛到 1 个注册点">

> 一句话：<选了哪个方案 + 一句话为什么是它>

### 3.1 系统现状
<架构 / 数据流图（ASCII 蓝图，`→` 表流转）；已有系统迭代 copy 旧图并标变更点 ⬅>

```
<数据流总图 ASCII 蓝图——关键图，有白板通道时评审版升级成白板，md 保留此蓝图同源>
```
> 图 1：<这张图证明了什么>

### 3.2 方案设计
<核心。多方案 → 选型对比表（设计/Pros/Cons）+ 明确结论 + 为什么（选中列打 ✅）。概念模型（表 + ER）。跨层数据流 ASCII 图（标包/服务边界）。变更点显眼标注 ⬅。>

> **设计要点**：<为什么这么选、为什么不那么选——每个关键决策都要有>

<!-- 图按阈值必画：调用链对象>3→时序图；实体状态>3→状态机；User>1&UseCase>5→用例图。
     先想清"这张图要证明哪句话"再选图型（diagram-cookbook.md）；每图配一句图注结论。 -->

## 4. 接口 / 协议定义
<仅当动接口。接口名/场景/入出参/字段注释 + 枚举值/错误码/兼容老版本/单位/限流·QPS。禁裸 dict/any。>

## 5. 存储设计
<仅当动 DB。DDL（字段注释齐全）+ 量级/分片 + 索引（不重复建、最左匹配）+ 大 key/热 key + 敏感加密。每表配设计要点。>

## 6. 监控与报警
<核心链路打点 + 报警规则 + 明确阈值（如"5min 失败>1 条""P99>500ms"）。metrics tag 低基数可枚举。>

## 7. 上线与回滚
- **上线顺序**：存储 → 权限 → 下游依赖 → 配置 → 代码（跨包接口是否需先合入）
- **灰度**：<尾号/白名单 1%→10%→50%→100%>
- **回滚**：<系统/接口/配置/数据；高风险配一键开关，改动全收敛进开关>

## 8. 工作量与排期（WBS）
| 模块 | 工作内容 | 人力(PD) | 负责人(唯一) | MR/交付时间 |
|---|---|---|---|---|
| | | | | |
<!-- 拆到 2–3PD/项，含部署/数据同步/DryRun/回滚等辅助任务 -->

## 9. 验收标准
<可量化、可独立验证、明确验收人；给 case + 上线后验证步骤（前端可用 Given-When-Then）>

## 10. 风险与对策
| 风险 | 对策 |
|---|---|
| <工程/安全/稳定性某风险> | <控制手段> |

<!-- 重型方案在此前后按需插入：术语说明 / 双向兼容性 / 强弱依赖矩阵 / 可观测性分层 / 分阶段实施 / 资损·对账·幂等 / 成本评估 -->

## 11. 待团队拍板 & 评审记录

> 一句话：<本次评审要拍的 N 件事各是什么>

**待团队拍板**（本次评审要拍的决策；Q 多时按维度分组，每条给倾向）：
- **Q1**：<背景 + 备选> — **倾向**：<你推荐哪个 + 一句话为什么>
- **Q2**：<...> — **倾向**：<...>

**评审记录**：
| 评审人 | 结论(通过/条件通过/不通过) | 时间 | 遗留 TODO |
|---|---|---|---|
| | | | |

**TODO**：
- [ ] <事项> — 跟进人 <@人> · DDL <日期>
